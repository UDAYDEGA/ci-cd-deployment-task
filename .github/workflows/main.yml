name: MuleSoft CI/CD - Deploy to All Environments

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:

  # ─────────────────────────────────────────────
  # JOB 1 - Build and publish to Exchange
  # Version auto-increments using GitHub run number
  # e.g. 1.0.1, 1.0.2, 1.0.3 ... on every push
  # ─────────────────────────────────────────────
  build:
    name: Build & Publish to Exchange
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: 17

      - name: Publish to Anypoint Exchange
        # revision = GitHub run number (auto-increments: 1, 2, 3...)
        # so version becomes 1.0.1, 1.0.2, 1.0.3 etc. on every push
        run: |
          mvn deploy --settings .maven/settings.xml -DskipMunitTests \
            -Drevision="${{ github.run_number }}" \
            -Danypoint.username="${{ secrets.ANYPOINT_USERNAME }}" \
            -Danypoint.password="${{ secrets.ANYPOINT_PASSWORD }}"

  # ─────────────────────────────────────────────
  # JOB 2 - Deploy to Sandbox
  # Same jar version, just different -Denv
  # ─────────────────────────────────────────────
  deploy-sandbox:
    name: Deploy to Sandbox
    needs: build
    runs-on: ubuntu-latest
    environment: Sandbox
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-
      - uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: 17

      - name: Deploy to Sandbox
        run: |
          mvn deploy --settings .maven/settings.xml -DskipMunitTests -DmuleDeploy \
            -Drevision="${{ github.run_number }}" \
            -Dconnected.app.client_id="${{ secrets.CONNECTED_APP_ID }}" \
            -Dconnected.app.client_secret="${{ secrets.CONNECTED_APP_SECRET }}" \
            -Danypoint.businessGroup="${{ secrets.BUSINESS_GROUP }}" \
            -Dplatform.client_id="${{ secrets.PLATFORM_CLIENT_ID }}" \
            -Dplatform.client_secret="${{ secrets.PLATFORM_CLIENT_SECRET }}" \
            -Denv="Sandbox"

  # ─────────────────────────────────────────────
  # JOB 3 - Deploy to Dev
  # ─────────────────────────────────────────────
  deploy-dev:
    name: Deploy to Dev
    needs: deploy-sandbox
    runs-on: ubuntu-latest
    environment: Dev
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-
      - uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: 17

      - name: Deploy to Dev
        run: |
          mvn deploy --settings .maven/settings.xml -DskipMunitTests -DmuleDeploy \
            -Drevision="${{ github.run_number }}" \
            -Dconnected.app.client_id="${{ secrets.CONNECTED_APP_ID }}" \
            -Dconnected.app.client_secret="${{ secrets.CONNECTED_APP_SECRET }}" \
            -Danypoint.businessGroup="${{ secrets.BUSINESS_GROUP }}" \
            -Dplatform.client_id="${{ secrets.PLATFORM_CLIENT_ID }}" \
            -Dplatform.client_secret="${{ secrets.PLATFORM_CLIENT_SECRET }}" \
            -Denv="Dev"

  # ─────────────────────────────────────────────
  # JOB 4 - Deploy to UAT
  # ─────────────────────────────────────────────
  deploy-uat:
    name: Deploy to UAT
    needs: deploy-dev
    runs-on: ubuntu-latest
    environment: UAT
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-
      - uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: 17

      - name: Deploy to UAT
        run: |
          mvn deploy --settings .maven/settings.xml -DskipMunitTests -DmuleDeploy \
            -Drevision="${{ github.run_number }}" \
            -Dconnected.app.client_id="${{ secrets.CONNECTED_APP_ID }}" \
            -Dconnected.app.client_secret="${{ secrets.CONNECTED_APP_SECRET }}" \
            -Danypoint.businessGroup="${{ secrets.BUSINESS_GROUP }}" \
            -Dplatform.client_id="${{ secrets.PLATFORM_CLIENT_ID }}" \
            -Dplatform.client_secret="${{ secrets.PLATFORM_CLIENT_SECRET }}" \
            -Denv="UAT"

  # ─────────────────────────────────────────────
  # JOB 5 - Deploy to Production
  # Requires manual approval in GitHub UI
  # ─────────────────────────────────────────────
  deploy-production:
    name: Deploy to Production
    needs: deploy-uat
    runs-on: ubuntu-latest
    environment: Production
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-
      - uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: 17

      - name: Deploy to Production
        run: |
          mvn deploy --settings .maven/settings.xml -DskipMunitTests -DmuleDeploy \
            -Drevision="${{ github.run_number }}" \
            -Dconnected.app.client_id="${{ secrets.CONNECTED_APP_ID }}" \
            -Dconnected.app.client_secret="${{ secrets.CONNECTED_APP_SECRET }}" \
            -Danypoint.businessGroup="${{ secrets.BUSINESS_GROUP }}" \
            -Dplatform.client_id="${{ secrets.PLATFORM_CLIENT_ID }}" \
            -Dplatform.client_secret="${{ secrets.PLATFORM_CLIENT_SECRET }}" \
            -Denv="Production"
